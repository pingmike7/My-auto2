name: AxentHost Auto Claim Final

on:
  schedule:
    - cron: "*/5 * * * *"   # 每 5 分钟触发，测试用
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduled run works"